package retângulo;
import java.awt.Color;
import java.awt.Graphics;

/**
 * Escreva uma descrição da classe RetanguloGr aqui.
 * 
 * @author (seu nome) 
 * @version (um número da versão ou uma data)
 */
public class RetanguloGr extends Retangulo {
    // ATRIBUTOS -- RETÂNGULO GRÁFICO
    Color corRetangulo = Color.BLACK;           // Cor padrão
    String nomeRetangulo = "";                  // Nome padrão
    //Color corNomeRetangulo  = Color.BLACK;    // Cor do nome padrão
    int espRetangulo = 1;                       // Espessura padrão
    
    // CONSTRUTORES (to-do)
    // ...
    
    // SETTERS E GETTERS (to-do)
    
    // OUTROS MÉTODOS (to-do)
}
